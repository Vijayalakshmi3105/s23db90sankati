//- extends layout

//- block content
//-   h1= title
//-   p Working on Pug 
//-   p My name is #{title}.
//-   button(type="button" class="btn btn-secondary" onclick="doReduce()") Press Me
//-   a(href='http://localhost:3000/vehicals') Link
//-   div#reduce Reduce the value
//-   script.
//-     let count = 20;
//-     let change = -1;
//-     function doReduce() {
//-       count += change;
//-       change -= 1;
//-       document.getElementById('reduce').textContent = `Count ${count} with change ${change}`;
//-     }
extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/vehicals") Take me to vehicles
    br
    a(href="/logout") Logout

