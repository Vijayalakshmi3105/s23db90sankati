extends layout

block content
  h1= title
  - var vi = results
  div(class="container-fluid")
  div(class="row")
   each v,index  in vi
    div(class="col-sm-2" onclick =`buy(${index})`) #{v.type}, #{v.milage}, #{v.price}
    script.
        function buy(a)
        {
            alert(`You purchased Item ${a+1}`)
        }